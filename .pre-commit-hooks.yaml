- id: prim
  name: prim
  description:
    Format Markdown, JSON/JSONC, YAML, and TOML, and apply whitespace hygiene
    to un-owned text files. Files prim does not own are skipped, not errored.
  entry: prim fmt
  language: system
  types: [text]
  minimum_pre_commit_version: "2.9.2"
