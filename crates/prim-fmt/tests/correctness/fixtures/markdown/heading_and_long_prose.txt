-- input --
#  Heading

Some   prose with `inline code` that runs on and on and on well past the wrap.

- one
- two
-- expected --
# Heading

Some prose with `inline code` that runs on and on and on well past the wrap.

- one
- two
